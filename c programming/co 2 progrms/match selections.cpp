#include<stdio.h>
int main()
{
	 int x;
	 printf("enter the value of x");
	scanf("%d",&x);
	if(x==0)
	{printf("india is selected to field");
	
	}
	else
	{
		printf("india is not selected to field");
	}
	return 0;
}
